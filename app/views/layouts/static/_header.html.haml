- unless @wpblog
  - if logged_in? 
    #admin
      %nav#admin_navigation{role: 'navigation'}
        .mobile
          %ul
            %li
              Logged in as #{current_user.name}
              %a#toggle_admin_navigation{href: '#'} ☰
          %ul#single_column_admin_navigation
            = render partial: 'layouts/static/admin_nav_items'
        .regular
          %ul
            %li
              Logged in as #{current_user.name}
            = render partial: 'layouts/static/admin_nav_items'
          .break
%header#pagehead{role: 'banner'}
  #banner
    = link_to image_tag("tekniklr.png", alt: DOMAIN, width: 242, height: 35), root_path
  #tagline #{TAGLINE}
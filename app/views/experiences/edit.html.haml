= stylesheet_link_tag 'experiences'

:ruby
  links = [
    link_to('experience list', experiences_path)
  ]
= scaffold_navigation(links: links, additional_class: 'mb-3', label: 'Experience navigation')

%h1 Editing experience

= render 'edit', experience: @experience

= scaffold_navigation(links: links, additional_class: 'mt-3', label: 'Experience navigation')
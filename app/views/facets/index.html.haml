- if @facets && @facets.count > 0
  %h2 Current facets
  #facets
  - @facets.each do |facet|
    = render 'show', :facet => facet

- if @facet
  %h2 New facet
  #notification
  = form_for :facet, :html => {:id => 'new_facet'}, :remote => true do    
    %div.facet#facet
      = fields_for @facet do |create|
        = render 'form', :f => create, :facet => @facet  
    .center
      = submit_tag "Add"
